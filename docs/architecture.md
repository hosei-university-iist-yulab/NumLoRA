# NumLoRA Architecture Diagrams

Reference for drawing paper figures. Two figures recommended:
1. **Figure 1 (full-page):** End-to-end pipeline comparing LoRA vs NumLoRA
2. **Figure 2 (half-page):** Zoomed-in NumLoRA layer with CTGS detail

---

## Figure 1: End-to-End Pipeline (LoRA vs NumLoRA)

This figure shows the full frozen-LLM imputation pipeline, with the LoRA path on top and the NumLoRA (CTGS) path on the bottom. The key visual difference is the CTGS backward hook on the LoRA-A gradient.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FROZEN-LLM IMPUTATION PIPELINE                     │
│                                                                       │
│  Raw Time Series          Patch Tokenization         Input Projection │
│  ┌───────────────┐       ┌─────────────────┐       ┌───────────────┐  │
│  │ x₁ x₂ ... xₜ │──────▶│  [p₁] [p₂] ... │──────▶│  W_proj · p   │  │
│  │ (T × D)       │       │  (n_patches ×   │       │  ────────────▶│  │
│  │               │       │   patch_size×D)  │       │  h₀ ∈ ℝᵈ     │  │
│  └───────────────┘       └─────────────────┘       └──────┬────────┘  │
│                                                           │           │
│                           MCAR Mask                       ▼           │
│                          (MR = 0.1,          ┌────────────────────┐   │
│                           0.3, 0.5)          │  FROZEN BACKBONE   │   │
│                                              │  (SmolLM / Qwen /  │   │
│                                              │   TinyLlama / Phi) │   │
│                                              │                    │   │
│                                              │  ┌──────────────┐  │   │
│                                              │  │  Layer 1     │  │   │
│                                              │  │  ┌────────┐  │  │   │
│                                              │  │  │ W₀ + ΔW│◄─┼──┼── │
│                                              │  │  └────────┘  │  │   │
│                                              │  │  Layer 2     │  │   │
│                                              │  │  ┌────────┐  │  │   │
│                                              │  │  │ W₀ + ΔW│◄─┼──┼── │
│                                              │  │  └────────┘  │  │   │
│                                              │  │     ...      │  │   │
│                                              │  │  Layer L     │  │   │
│                                              │  │  ┌────────┐  │  │   │
│                                              │  │  │ W₀ + ΔW│◄─┼──┼── │
│                                              │  │  └────────┘  │  │   │
│                                              │  └──────────────┘  │   │
│                                              └────────┬───────────┘   │
│                                                       │               │
│                                                       ▼               │
│                                              ┌────────────────┐       │
│                                              │  Output Head   │       │
│                                              │  h_L → ŷ       │       │
│                                              └────────┬───────┘       │
│                                                       │               │
│                                                       ▼               │
│                                              ┌────────────────┐       │
│                                              │ Masked MSE     │       │
│                                              │ L(ŷ, y, mask)  │       │
│                                              └────────────────┘       │
└─────────────────────────────────────────────────────────────────────────┘

     WHERE ΔW DIFFERS:

     ┌──────────────────────┐     ┌──────────────────────────────────┐
     │     Standard LoRA    │     │         NumLoRA (CTGS)           │
     │                      │     │                                  │
     │   ΔW = B · A         │     │   ΔW = B · A    (same forward)  │
     │                      │     │                                  │
     │   Backward:          │     │   Backward:                     │
     │   ∇A = B⊤ · ∇h · x⊤ │     │   ∇A = B⊤ · ∇h · x⊤            │
     │                      │     │        · c_ℓ / (||x|| + ε)      │
     │   (no modification)  │     │        ▲                         │
     │                      │     │        │ CTGS hook               │
     │                      │     │        │ (1 scalar per layer)    │
     └──────────────────────┘     └──────────────────────────────────┘
```

---

## Figure 2: NumLoRA Layer Detail (CTGS Focus)

This figure zooms into a single adapted layer, showing the forward and backward paths side by side. The CTGS hook is the only modification.

```
                        SINGLE ADAPTED LAYER ℓ

    ════════════════ FORWARD PASS (identical to LoRA) ════════════════

                              x_ℓ (input activation)
                                │
                    ┌───────────┼───────────┐
                    │           │           │
                    ▼           │           ▼
             ┌───────────┐     │    ┌─────────────┐
             │   W₀      │     │    │  A_ℓ        │
             │  (frozen)  │     │    │  (r × d_in) │
             │           │     │    └──────┬──────┘
             │           │     │           │
             │           │     │           ▼
             │           │     │    ┌─────────────┐
             │           │     │    │  B_ℓ        │
             │           │     │    │  (d_out × r)│
             └─────┬─────┘     │    └──────┬──────┘
                   │           │           │
                   │           │           │
                   ▼           │           ▼
              ┌─────────────────────────────────┐
              │          h = W₀x + BAx          │
              └──────────────┬──────────────────┘
                             │
                             ▼
                        h_ℓ (output)


    ════════════════ BACKWARD PASS (CTGS difference) ════════════════

                         ∇h_ℓ (from loss)
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            │            ▼
    ┌──────────────┐       │    ┌──────────────────┐
    │ ∇W₀ = 0     │       │    │ ∇B = ∇h · (Ax)⊤  │
    │ (frozen,     │       │    │ (standard)        │
    │  no update)  │       │    └──────────────────┘
    └──────────────┘       │
                           │
                           ▼
                 ┌──────────────────┐
                 │ ∇A = B⊤∇h · x⊤  │  ◄── raw gradient (same as LoRA)
                 └────────┬─────────┘
                          │
                ┌─────────▼──────────┐
                │                    │
                │   CTGS HOOK        │  ◄── THE ONLY DIFFERENCE
                │                    │
                │   ∇A ← ∇A · ──c_ℓ─── │
                │              ||x||+ε  │
                │                    │
                │   c_ℓ: learnable   │
                │   (init = 1.0)     │
                │                    │
                │   ||x||: input     │
                │   activation norm  │
                │   (cached from     │
                │    forward pass)   │
                │                    │
                └─────────┬──────────┘
                          │
                          ▼
                 ┌──────────────────┐
                 │  AdamW update    │
                 │  A ← A - lr·∇A  │
                 │  c ← c - lr_c·∇c│
                 └──────────────────┘
```

---

## Figure 3: The Gradient Mismatch Problem (Motivation)

This figure visually explains WHY CTGS is needed. Side-by-side comparison of text vs numerical gradient behavior.

```
    ═══════ TEXT (LoRA works fine) ═══════     ═══ NUMERICAL (LoRA breaks) ═══

    Token: "the"    Token: "cat"               Patch: [300K, 1atm]   Patch: [70bpm, 0.95]
        │               │                          │                      │
        ▼               ▼                          ▼                      ▼
    ┌────────┐      ┌────────┐                ┌─────────┐           ┌─────────┐
    │Embed   │      │Embed   │                │W_proj · │           │W_proj · │
    │lookup  │      │lookup  │                │[300,1]  │           │[70,0.95]│
    └───┬────┘      └───┬────┘                └────┬────┘           └────┬────┘
        │               │                          │                     │
    ||x|| ≈ 0.5     ||x|| ≈ 0.5              ||x|| ≈ 45.2          ||x|| ≈ 3.1
        │               │                          │                     │
    ┌───┴───┐       ┌───┴───┐                 ┌───┴────┐           ┌────┴───┐
    │ ∇A ∝  │       │ ∇A ∝  │                 │ ∇A ∝   │           │ ∇A ∝   │
    │ 0.5   │       │ 0.5   │                 │ 45.2   │           │ 3.1    │
    └───┬───┘       └───┬───┘                 └───┬────┘           └───┬────┘
        │               │                         │                    │
        ▼               ▼                         ▼                    ▼
    ┌─────────────────────────┐           ┌──────────────────────────────┐
    │  Similar magnitude      │           │  15× difference!             │
    │  → stable training      │           │  → temp patch dominates      │
    │                         │           │  → unstable updates          │
    └─────────────────────────┘           └──────────────────────────────┘

                                                     │
                                                     ▼ CTGS FIX
                                          ┌──────────────────────┐
                                          │  ∇A ← ∇A · c/||x||  │
                                          │                      │
                                          │  45.2 · c/45.2 = c  │
                                          │  3.1  · c/3.1  = c  │
                                          │                      │
                                          │  → Equal magnitude   │
                                          │  → Stable training   │
                                          └──────────────────────┘
```

---

## Figure 4: Activation Variance (Text vs Numbers)

Bar chart or histogram showing per-layer activation variance. This is empirical data from our experiments.

```
    Per-Layer Activation Variance: Text vs Numerical on SmolLM-360M

    Text (WikiText-103):                  Numerical (ETT-h1):
    Layer  Variance                       Layer  Variance
    ─────────────────                     ─────────────────
    1      0.92   ████████                1      0.001  ▏
    2      1.05   █████████               2      42.3   ███████████████████
    3      0.88   ████████                3      155.0  ██████████████████████████████████████
    4      1.12   █████████               4      89.7   ████████████████████████████
    5      0.95   ████████                5      12.1   ██████████
    ...    ...                            ...    ...
    32     0.98   ████████                32     0.03   ▎

    Range: 0.8 — 1.2                     Range: 0.001 — 155
    Factor: 1.5×                         Factor: 155,000×
                                         ─────────────────
                                         LoRA's fixed init assumes
                                         constant variance → BROKEN
                                         for 5 orders of magnitude
```

---

## Drawing Instructions for Final Figures

### For Figure 1 (paper main figure):
- Use a clean vector tool (Figma, draw.io, or TikZ)
- Two-column layout: left = pipeline flow, right = LoRA vs NumLoRA comparison box
- Color coding: blue = frozen (W₀), red = trainable (A, B), green = CTGS (c_ℓ)
- Frozen parts should be grayed out / dashed borders
- CTGS hook should be highlighted with a colored box or callout
- Keep it at single-column width for NeurIPS format

### For Figure 2 (zoomed layer):
- Show forward path as solid arrows, backward path as dashed arrows
- The CTGS hook should be a distinct colored box breaking the backward arrow
- Label the equation inline: ∇A ← ∇A · c/(||x||+ε)
- Show c_ℓ as a small orange circle with "learnable" label

### For Figure 3 (motivation):
- Side-by-side comparison, text on left, numbers on right
- Use concrete examples (temperature 300K, heart rate 70bpm)
- Show the gradient magnitude bars visually (thick vs thin)
- After CTGS: both bars become equal thickness

### Color Palette:
- Frozen weights: #B0B0B0 (gray)
- LoRA A, B: #1f77b4 (blue)
- CTGS c_ℓ: #2ca02c (green)
- Gradient flow: #d62728 (red arrows)
- Input/output: #ff7f0e (orange)

### Recommended Paper Layout:
- Figure 1: top of Section 3 (Method), spans full column width
- Figure 3: in Section 3.1 (Gradient Mismatch Problem), half-column
- Figure 4: in Section 5 (Analysis), half-column or moved to appendix
- Figure 2: optional, can be appendix if space is tight
